
<div class="container">
  <h1>Les dernières nouveautés</h1>
    <ul>

      <% @nouveautes.map do |nouveaute| %>

        <p><%= Nouveaute.last(3)[0][:citation]%></p>
        <p><%= Nouveaute.last(3)[1][:citation]%></p>
        <p><%= Nouveaute.last(3)[2][:citation]%></p>


       <%# <p><%= nouveaute.citation %>
        <%= link_to "Supprimer", nouveaute_path(nouveaute), method: :delete, class: 'btn btn-danger' %>
      <% end %>
    </ul>
</div>

      <%# <%= link_to "aller voir", artiste_nouveautes_path(nouveaute), class: 'btn btn-primary' %>


